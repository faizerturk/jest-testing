//toBe();

test("two plus two is four", () => {
  expect(2 + 2).toBe(4);
});

// toBe() is a function used to compare two values to check if they are equal. It is used for comparing simple data types like numbers, strings, and booleans.
